<% @conversations.each do |conversation| %>
<% recipient = conversation.recipient_id %>
<p><%= link_to "#{User.find_by(id: recipient).name}", user_conversation_path(current_user, conversation) %></p>
<% end %>
