<h1>Edit the Image</h1>

<%= form_for @image do |f| %>
  <%= f.label "Image Name" %>
  <%= f.text_field :name %>
  <%= f.collection_select :mood_id, Mood.all, :id, :name %>
  <%= f.submit %>
<% end %>
